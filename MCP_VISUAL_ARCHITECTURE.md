# MCP vs mcp-cli: Visual Architecture Guide

## High-Level Architecture Comparison

### Traditional MCP HTTP-Based Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        AI Agent/Client                      │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
            ┌────────────────────────────┐
            │  Load ALL Tool Schemas     │
            │  • add_numbers             │
            │  • subtract_numbers        │
            │  • divide_numbers          │
            │  • get_current_time        │
            │  • ... (47 more tools)     │
            │                            │
            │  Token Cost: ~47,000 🔴    │
            └────────────┬───────────────┘
                         │ HTTP POST
                         ▼
            ┌────────────────────────────┐
            │   FastAPI HTTP Server      │
            │   (app.py)                 │
            │                            │
            │  POST /mcp/tools/list      │
            │  POST /mcp/tools/call      │
            │  POST /mcp/initialize      │
            └────────────────────────────┘
                         │
                         ▼
            ┌────────────────────────────┐
            │    Tool Implementations    │
            │      (tools.py)            │
            └────────────────────────────┘
```

**Problem:** All tool schemas loaded upfront = massive token consumption

---

### mcp-cli Dynamic Discovery Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        AI Agent/Client                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
         ▼             ▼             ▼
    ┌────────┐  ┌──────────┐  ┌──────────┐
    │ List   │  │ Get Info │  │  Call    │
    │Servers │  │  Tool    │  │  Tool    │
    │        │  │          │  │          │
    │~100    │  │~200      │  │~300      │
    │tokens  │  │tokens    │  │tokens    │
    └───┬────┘  └────┬─────┘  └────┬─────┘
        │            │             │
        └────────────┼─────────────┘
                     │ stdin/stdout
                     ▼
         ┌─────────────────────────┐
         │     mcp-cli Runner      │
         │                         │
         │  stdin ← {request}      │
         │  stdout → {response}    │
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ Python Stdio MCP Server │
         │   (mcp_server.py)       │
         │                         │
         │ handle_initialize()     │
         │ handle_list_tools()     │
         │ handle_call_tool()      │
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ Tool Implementations    │
         │   (tools.py)            │
         └─────────────────────────┘

Total Token Cost: ~600 🟢 (99% savings!)
```

**Advantage:** Load only what you need, when you need it

---

## Request/Response Flow Diagrams

### Traditional MCP: Get All Tools

```
Client                              Server
  │                                   │
  ├──────────────────────────────────►│ POST /mcp/tools/list
  │                                   │
  │                              ┌────┴────────────────┐
  │                              │ Build all schemas:  │
  │                              │ - add_numbers       │
  │                              │ - subtract_numbers  │
  │                              │ - divide_numbers    │
  │                              │ - get_current_time  │
  │                              │ (+ more)            │
  │                              └────┬────────────────┘
  │                                   │
  │◄──────────────────────────────────┤ 47,000 tokens
  │      { tools: [4+ schemas] }       │
  │                                   │
```

### mcp-cli: Get Specific Tool

```
Client (mcp-cli)                    Server
  │                                   │
  ├──────────────────────────────────►│ stdin: { method: "tools/list" }
  │                                   │
  │                              ┌────┴────────────────┐
  │                              │ Return tool names   │
  │                              │ (minimal)           │
  │                              └────┬────────────────┘
  │                                   │
  │◄──────────────────────────────────┤ ~100 tokens
  │ stdout: { tools: ["name1", ...] } │
  │                                   │
  │─ Client analyzes and chooses ─────┐
  │                                   │
  ├──────────────────────────────────►│ stdin: { method: "tools/call",
  │                                   │         name: "add_numbers" }
  │                              ┌────┴────────────────┐
  │                              │ Execute tool        │
  │                              └────┬────────────────┘
  │                                   │
  │◄──────────────────────────────────┤ ~300 tokens
  │ stdout: { result: { ... } }       │
  │                                   │
```

---

## Protocol Comparison

### Traditional MCP (HTTP/REST)

```
┌─────────────────────────────────────────────────────────────┐
│                     HTTP Requests                            │
├─────────────────────────────────────────────────────────────┤
│ Method      │ Endpoint              │ Response              │
├─────────────┼───────────────────────┼──────────────────────┤
│ POST        │ /mcp/initialize       │ Server capabilities  │
│ POST        │ /mcp/tools/list       │ All tool schemas     │
│ POST        │ /mcp/tools/call       │ Tool result          │
│ GET         │ /mcp                  │ Server info          │
└─────────────┴───────────────────────┴──────────────────────┘

Structure: JSON-RPC 2.0 over HTTP

Pros:
  ✅ Standard HTTP
  ✅ Easy to debug (curl)
  ✅ Full control

Cons:
  ❌ All schemas loaded upfront
  ❌ High token cost
  ❌ Server must be running
```

### mcp-cli Stdio Protocol

```
┌─────────────────────────────────────────────────────────────┐
│                  Stdin/Stdout Protocol                       │
├─────────────────────────────────────────────────────────────┤
│ Request (stdin)  → Process → Response (stdout)              │
├─────────────────────────────────────────────────────────────┤
│ { "method": "initialize" }      → { "result": {...} }      │
│ { "method": "tools/list" }      → { "result": {...} }      │
│ { "method": "tools/call",       → { "result": {...} }      │
│   "params": {...} }             →                           │
└─────────────────────────────────────────────────────────────┘

Structure: JSON-RPC 2.0 over stdin/stdout

Pros:
  ✅ Dynamic discovery
  ✅ Low token cost
  ✅ No server to run
  ✅ mcp-cli compatible

Cons:
  ❌ Stdin/stdout protocol
  ❌ Harder to debug manually
```

---

## Information Load Timeline

### Traditional MCP: All at Once

```
Timeline ──────────────────────────────────────────────────────►

              T0 (Client starts)
              │
              ▼
        ┌──────────────┐
        │ Request all  │
        │ tool schemas │
        └──────────────┘
              │
              ▼ (Wait for HTTP)
        ┌──────────────────────────┐
        │ Receive 47k tokens worth │  ◄── ALL tools loaded
        │ of tool definitions      │
        │ (even if never used!)    │
        └──────────────────────────┘
              │
              ▼
        ┌──────────────┐
        │ Ready to use │  ◄── Can now select tools
        │ tools        │
        └──────────────┘

Context Consumption: 47,000 tokens (upfront)
```

### mcp-cli: On-Demand

```
Timeline ──────────────────────────────────────────────────────►

              T0 (Client starts)
              │
              ▼
        ┌──────────────┐
        │ Ask: What    │  ◄── ~100 tokens
        │ servers?     │
        └──────────────┘
              │
              ▼
        ┌──────────────┐
        │ Agent        │
        │ analyzes...  │
        └──────────────┘
              │
              ▼ (Only if needed)
        ┌──────────────┐
        │ Ask: Info    │  ◄── ~200 tokens (only for chosen tool)
        │ for tool X?  │
        └──────────────┘
              │
              ▼
        ┌──────────────┐
        │ Call tool X  │  ◄── ~300 tokens
        └──────────────┘

Context Consumption: ~600 tokens (on-demand)
Savings: 99% 🚀
```

---

## Component Interaction Diagrams

### Our Example Project: Full Stack

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              tools.py (Shared Implementation)         │   │
│  │                                                       │   │
│  │  def add_numbers(a, b): return {"result": a+b}      │   │
│  │  def subtract_numbers(a, b): ...                     │   │
│  │  def divide_numbers(a, b): ...                       │   │
│  │  def get_current_time(): ...                         │   │
│  │                                                       │   │
│  └───────────────────────────────────────────────────────┘   │
│                            ▲                                   │
│           ┌────────────────┼────────────────┐               │
│           │                │                │               │
│           ▼                ▼                ▼               │
│  ┌─────────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │  app.py         │ │mcp_server.py │ │ mcp-cli     │   │
│  │ (FastAPI)       │ │ (Stdio)      │ │ (CLI Tool)  │   │
│  │                 │ │              │ │             │   │
│  │ HTTP Endpoints  │ │ Stdin/Stdout │ │ Commands:   │   │
│  │ ────────────────│ │ ────────────│ │ ─────────── │   │
│  │ POST /mcp/*     │ │ initialize  │ │ list        │   │
│  │                 │ │ list tools  │ │ info        │   │
│  │ localhost:8000  │ │ call tool   │ │ call        │   │
│  │ (HTTP)          │ │ (process)   │ │ grep        │   │
│  └─────────────────┘ └──────────────┘ └──────────────┘   │
│        │                   │                │              │
│        ▼                   ▼                ▼              │
│  Direct HTTP         Direct Process      Dynamic CLI     │
│  Integration         Integration         Interface       │
│                                                           │
│  Use Case:           Use Case:           Use Case:       │
│  Custom Tools        mcp-cli Ready       AI Agents       │
│  Full Control        Token Efficient     99% Savings     │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

---

## Decision Flowchart

```
                            Start
                              │
                              ▼
              ┌───────────────────────────────┐
              │ Building for AI Agents?       │
              └───────┬───────────────────┬───┘
                    Yes               No
                      │                 │
                      ▼                 ▼
              ┌─────────────────┐ ┌──────────────────┐
              │ Use mcp-cli     │ │ Direct MCP OK    │
              │ (Recommended)   │ │                  │
              │                 │ ▼                  │
              │ 99% token       └──────────────────┘
              │ savings          └─────────────┬────┘
              │                               │
              │      (Or use BOTH)            ▼
              │      ├─ app.py (HTTP)     ┌────────┐
              │      └─ mcp_server.py     │ Choose:│
              │        (mcp-cli)          ├────────┤
              │                           │HTTP    │
              ▼                           │Endpoints
          ┌──────────────┐               └────────┘
          │ Implement:   │                   │
          │ mcp_server.py│                   ▼
          │ (Stdio)      │          ┌──────────────────┐
          │              │          │ Implement:       │
          │ $ python     │          │ app.py           │
          │ mcp_server.py           │ (FastAPI)        │
          │              │          │                  │
          │ $ mcp-cli    │          │ $ uvicorn app:app
          │ -c config    │          │                  │
          │              │          │ $ curl localhost │
          └──────────────┘          └──────────────────┘
```

---

## Token Usage Visualization

### 60 Tools Scenario

```
Traditional MCP:
┌─────────────────────────────────────────────────────────────┐
│████████████████████████████████████████████████████████████│
│ ~47,000 tokens (ALL tools loaded at once)                  │
│ Even if you only use 1 tool!                               │
└─────────────────────────────────────────────────────────────┘

mcp-cli Typical Usage:
┌──────────────────────────────────────────────────────────────┐
│████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│
│ ~600 tokens (list + inspect + execute)                     │
│ 99% SAVINGS!                                               │
└──────────────────────────────────────────────────────────────┘

Scenarios:
├─ List servers: 100 tokens
├─ Inspect 1 tool: 200 tokens  
└─ Execute tool: 300 tokens
```

---

## Summary Table

```
┌─────────────────────┬──────────────────┬────────────────────┐
│ Aspect              │ Traditional MCP  │ mcp-cli            │
├─────────────────────┼──────────────────┼────────────────────┤
│ Protocol            │ HTTP/REST        │ Stdio JSON-RPC     │
│ Token Cost          │ ~47,000          │ ~600               │
│ Savings             │ —                │ 99% 🚀             │
│ Setup               │ Medium           │ Low                │
│ Server Required     │ Yes              │ No                 │
│ Best For            │ Direct APIs      │ AI Agents          │
│ Discovery           │ Static (all)     │ Dynamic (on-demand)│
│ Scaling             │ Limited          │ Unlimited          │
│ Debugging           │ Easy (curl)      │ Moderate           │
│ Production Ready    │ Yes              │ Yes ✅             │
└─────────────────────┴──────────────────┴────────────────────┘
```

---

**Visual Guide Created:** February 2026  
**Example Project:** Simple Calculator MCP Server with both implementations
